#!/usr/bin/env bash
#MISE description="Extract content after frontmatter"
#USAGE arg "<file>" help="Markdown file to read"

set -euo pipefail
exec uv run --project "$MISE_CONFIG_ROOT" python3 "$MISE_CONFIG_ROOT/lib/farts.py" body "$@"
